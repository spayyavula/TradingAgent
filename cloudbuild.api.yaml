steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - -f
      - Dockerfile.api
      - -t
      - ${_IMAGE}
      - .
  - name: gcr.io/cloud-builders/docker
    args: [push, "${_IMAGE}"]

substitutions:
  _IMAGE: ""

images:
  - ${_IMAGE}

options:
  logging: CLOUD_LOGGING_ONLY
